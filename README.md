WIP, don't read too much into anything here just yet.

Requirements:
- libspdlog-dev
- cmake

Build and run:
```bash
# Build
mkdir build
cd build
cmake ..
make

# Run
./app/example_app/example_app [options]
```